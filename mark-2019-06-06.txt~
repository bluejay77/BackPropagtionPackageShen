Hi,,

About the backpropagation software--

I attach the ld.shen file, whose function is to load the entire
backpropagation software.

The whole system loads with no errors, but work needs to be done with
it.

